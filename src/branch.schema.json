{
  "description": "run sub-agent that see current conversation history and can use tools. Use it to split big task into multiple - delegating work to sub-agents. Or use it to save context space by delegating simple tasks that requires a lot of tool calls",
  "parameters": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "specific instruction for the sub agent"
      }
    },
    "required": ["text"]
  }
}
