{
  "description": "Execute a powershell.exe command",
  "parameters": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "The command to execute"
      }
    },
    "required": ["text"]
  }
}
