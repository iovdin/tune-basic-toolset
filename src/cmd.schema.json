{
  "description": "Execute a cmd.exe command",
  "parameters": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "The command to execute"
      }
    },
    "required": ["text"]
  }
}
